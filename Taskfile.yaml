version: '3'

dotenv: ['custom.env', 'default.env']

vars:
  AWESOME_FILE: ../doap/README.md
  BASE_IRI: https://aksw.org/Projects/
  OUTPUT: ../graph/
  NT: ../doap/aksw.org.nt

tasks:
  default:
    desc: List all tasks
    cmds:
      - task -a

  md:gen:
    desc: Generate Markdown file
    cmds:
      - python3 ./doap/extract_script.py {{.URL}}


  install:
    desc: Install the dependencies with poetry
    cmds:
      - poetry install
      - pipx install kisumu

  run:
    desc: Execute the command
    cmds:
      - poetry run python3 -m waag.cli --awesome-file {{.AWESOME_FILE}} --iri {{.BASE_IRI}} --output-dir {{.OUTPUT}} --graph-nt {{.NT}}

  test:
    desc: Run tests
    cmds:
      - poetry run pytest

  format:
    desc: Format the code
    aliases: ["ruff"]
    cmds:
      - poetry run ruff format
      - poetry run ruff check --fix

  build:
    desc: Build the README
    cmds:
      - kisumu build -g graphs/awesome.ttl -t templates/README.md.jinja -o README.md
